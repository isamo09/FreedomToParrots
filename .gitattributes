# Placeholder core binaries (see internal/corebin) must never be touched by
# line-ending conversion - real releases overwrite them at build time, but
# the placeholder bytes committed here need to stay byte-for-byte stable.
internal/corebin/bin/core -text
internal/corebin/bin/core.exe -text
